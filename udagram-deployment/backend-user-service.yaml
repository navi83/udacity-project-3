apiVersion: v1
kind: Service
metadata:
  name: backend-user
  labels:
    run: backend-user
spec:
  ports:
  - port: 8080
    name: port1
    protocol: TCP
  - port: 8100
    name: port2
    protocol: TCP
  selector:
    run: backend-user